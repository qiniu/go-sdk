# 复制为 .env 后填入实际值，使用 `source .env` 加载到当前 shell。

# 七牛沙箱 API Key（必填）
export QINIU_API_KEY=

# 沙箱 API 地址（可选，默认使用 SDK 内置 Endpoint）
export QINIU_SANDBOX_API_URL=

# Anthropic API Key（必填，将通过环境变量注入到沙箱）
export ANTHROPIC_API_KEY=

# 第三方网关地址（可选，例如 https://api.qnaigc.com）
export ANTHROPIC_BASE_URL=
