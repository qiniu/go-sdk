运行提交前检查：代码格式化验证、静态检查和单元测试。

执行步骤：

1. 运行 `gofmt -s -l .` 检查未格式化的文件，如果有未格式化文件则列出并提示运行 `gofmt -s -w .` 修复
2. 运行 `make staticcheck` 进行静态检查
3. 运行 `make unittest` 运行单元测试

如果任何步骤失败，停止并报告错误，给出修复建议。
全部通过后输出简洁的通过确认。
